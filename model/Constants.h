#ifndef CONSTANTS_H
#define CONSTANTS_H

enum Implementation {
    RANDOM, PARITY
};

enum Position {
    QUEEN = 1
};

enum ConsoleColor {
    BLUE = 63,
    WHITE = 7
};

const unsigned int SIMULATIONS = 100;
const unsigned int N = 64;

#endif
