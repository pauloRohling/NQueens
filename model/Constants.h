#ifndef CONSTANTS_H
#define CONSTANTS_H

#include <vector>

enum Implementation {
    RANDOM, PARITY
};

enum Position {
    QUEEN = 1
};

enum ConsoleColor {
    BLUE = 63,
    WHITE = 7
};

const unsigned int SIMULATIONS = 100;

#endif
